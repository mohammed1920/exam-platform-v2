# دليل المطور - منصة الاختبارات القانونية V2 ⚖️

تم تحديث هذه المنصة لتكون أكثر سرعة، استقراراً، وسهولة في الإدارة. يعتمد النظام على ملفات JSON ثابتة يتم معالجتها عبر محرك اختبارات ذكي (Exam Engine).

## 🚀 كيفية إضافة أسئلة جديدة

1. **إضافة ملف JSON**: أضف ملفك في مجلد `data/` داخل المجلد الفرعي المناسب (مثلاً: `data/law_admin/chapter_3.json`).
2. **تنسيق الملف**: يجب أن يتبع التنسيق التالي:
   ```json
   {
     "id": "unique_id",
     "book_id": "book_slug",
     "chapter": 1,
     "title": "عنوان الفصل",
     "questions": [
       {
         "q": "نص السؤال؟",
         "options": ["خيار 1", "خيار 2", "خيار 3", "خيار 4"],
         "correct": 0,
         "explanation": "شرح اختياري"
       }
     ]
   }
   ```
3. **تحديث الفهرس**: لا حاجة لتعديل `books.json` يدوياً. فقط قم بتشغيل سكريبت التحديث:
   ```bash
   python3 update_questions.py update
   ```

## 🤖 الأتمتة (GitHub Actions)

المنصة مزودة بنظام تحديث تلقائي يعمل في الحالات التالية:
- **تلقائياً**: كل يوم عند منتصف الليل (00:00 UTC).
- **يدوياً**: يمكنك تشغيله من تبويب **Actions** في GitHub عبر اختيار "تحديث المنصة التلقائي".
- **عند الدفع**: أي تغيير ترفعه للمستودع سيؤدي لتحديث الموقع فوراً.

## 🛠️ سكريبت الإدارة (`update_questions.py`)

هذا السكريبت هو "العقل المدبر" لإدارة البيانات:
- يقوم بإضافة معرفات (IDs) فريدة للأسئلة المفقودة.
- يتأكد من صحة هيكلية الملفات.
- يحدث فهرس الكتب (`books.json`) بناءً على المجلدات الموجودة.

## 📱 ميزات واجهة المستخدم

- **الوضع الليلي**: مفعل افتراضياً بتصميم أنيق.
- **دعم الزر الخلفي**: تم تحسين التنقل في الجوال ليعمل زر "الرجوع" داخل التطبيق بدلاً من الخروج من الصفحة.
- **البحث الذكي**: يمكن البحث عن الكتب أو الفصول من القائمة الرئيسية.

---
**إعداد: Manus AI** 🦾
