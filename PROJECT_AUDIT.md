# تقرير إصلاح المشروع

## النتيجة

تم إصلاح السبب الرئيسي لاختفاء الكتب، مع إضافة طبقة سلامة للبيانات والأمان بدون تعديل المحتوى القانوني للأسئلة.

### المشكلة الرئيسية السابقة

`update_questions.py` كان يعيد بناء `data/books.json` من مجلدات تبدأ بـ `law_` ويشترط وجود فصل واحد على الأقل. لذلك الكتاب الذي يُضاف من لوحة الإدارة بـ `chapters: 0` كان يظهر أولاً، ثم يحذفه GitHub Action عند أول تحديث لاحق للبيانات.

### الإصلاح

- `books.json` هو المصدر الرئيسي لقائمة الكتب.
- الـworkflow لا يحذف الكتب.
- `chapters` يُحسب من ملفات `chapter_N.json` الموجودة فعليًا.
- الكتاب الجديد بدون فصول يبقى في القائمة.
- عند إضافة/حذف فصل، يحدث العدد تلقائيًا.
- تم إلغاء اعتماد `index.html` كمصدر ثانٍ للكتب.

## هوية الأسئلة

- تم الحفاظ على `id` الموجود لكل سؤال.
- تم إضافة `uid` ثابت وفريد لكل سؤال.
- تم تحديث تقارير QA والإملاء لتحتوي `question_uid`.
- تعديل السؤال لا يستبدل `id` ولا `uid` ولا يمسح `explanation`.
- الروابط المباشرة تستخدم `uid` عندما يكون موجودًا.
- الاختبار الحالي: 6245 سؤالًا، 6245 `uid` فريدًا، ولا يوجد `uid` مفقود أو مكرر.

## محرر الخيارات

المحرر القديم كان يفرض أربعة خيارات، بينما البيانات الحالية تحتوي أسئلة بـ 1 إلى 10 خيارات. أصبح المحرر يحافظ على العدد الفعلي للخيارات ويمكنه إضافة/حذف الخيارات دون إسقاط الخيارات القديمة.

## الأتمتة

الأتمتة تقوم بالأعمال الميكانيكية فقط:

- إصلاح هوية مفقودة للسؤال.
- تحديث عدد الفصول.
- إنشاء تقرير سلامة غير هدّام.
- فحص بنيوي قبل النشر.
- تقارير AI للفحص اللغوي والجودة دون تعديل السؤال تلقائيًا.

أما الأمور التي قد تغيّر المعنى القانوني أو صحة السؤال فتبقى للمراجعة البشرية.

## الأمان والخصوصية

- لا يوجد GitHub Token داخل المصدر.
- لوحة الإدارة لا تخزن Token في تخزين المتصفح.
- اسم مالك GitHub غير موجود داخل كود لوحة الإدارة؛ يتم معرفة الحساب من `/user` بعد إدخال Token.
- الموقع العام لا ينشر لوحة الإدارة أو ملفات Python أو التقارير أو ملفات CI.
- تمت معالجة مسارات HTML الديناميكية الحساسة من XSS.
- الروابط الخارجية العامة تقبل HTTPS فقط.
- لا يوجد كود في المشروع يجمع IP الزائر أو يحفظه في ملفات البيانات.

**تنبيه:** إذا كان مستودع GitHub Public فالكود المصدر يظل ظاهرًا في GitHub حتى لو لم ننشره عبر Pages. لإخفاء المصدر نفسه يجب جعل المستودع Private وفق خطة GitHub المناسبة.

## ملاحظات تحتاج مراجعة بشرية الآن

تم توليد `reports/data_integrity_report.json`، ويحتوي حاليًا على:

- 15 سؤالًا لديه خيار واحد فقط؛ لم يتم اختراع خيارات تلقائيًا.
- مجلدين قديمين غير مرتبطين بـ `books.json`: `Law-gh` و`Law-moh`؛ لم يتم حذفهما.

هذه هي النقاط التي لا ينبغي للنظام أن يخمن فيها أو يصلحها قانونيًا من تلقاء نفسه.
