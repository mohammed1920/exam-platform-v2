# دليل التطوير - منصة الاختبارات القانونية V2

## 📚 نظرة عامة

هذا الدليل يشرح كيفية العمل مع المنصة، إضافة أسئلة جديدة، وتحسين الأداء.

---

## 🚀 البدء السريع

### 1. إضافة كتاب جديد

```bash
# أنشئ مجلد جديد للكتاب
mkdir -p data/law_your_book_name

# أنشئ ملف الفصل الأول
touch data/law_your_book_name/chapter_1.json
```

### 2. صيغة الفصل (Chapter Format)

كل ملف فصل يجب أن يتبع هذا الهيكل:

```json
{
  "questions": [
    {
      "id": 1,
      "q": "نص السؤال هنا؟",
      "options": [
        "الخيار الأول",
        "الخيار الثاني",
        "الخيار الثالث",
        "الخيار الرابع"
      ],
      "correct": 0,
      "explanation": "شرح الإجابة الصحيحة"
    },
    {
      "id": 2,
      "q": "سؤال آخر؟",
      "options": ["أ", "ب", "ج", "د"],
      "correct": 2,
      "explanation": "شرح إضافي"
    }
  ]
}
```

**ملاحظات مهمة:**
- `id`: رقم فريد للسؤال
- `q`: نص السؤال
- `options`: مصفوفة الخيارات (يجب أن تكون 2-6 خيارات)
- `correct`: فهرس الإجابة الصحيحة (0 = الخيار الأول)
- `explanation`: شرح الإجابة الصحيحة

### 3. تحديث الفهرس

بعد إضافة أسئلة جديدة، قم بتشغيل:

```bash
python3 update_questions.py update
```

هذا الأمر سيقوم بـ:
- ✓ البحث عن مجلدات الكتب الجديدة
- ✓ عد الفصول في كل كتاب
- ✓ تحديث ملف `books.json` تلقائياً
- ✓ التحقق من صحة جميع الأسئلة

### 4. التحقق من الأسئلة

للتحقق من صحة البيانات دون تحديث:

```bash
python3 update_questions.py validate
```

---

## 🔧 التخلص من الكاش (Cache Busting)

المنصة تستخدم تقنية **Cache Busting** لضمان تحميل أحدث الأسئلة دائماً:

```javascript
// عند تحميل الأسئلة، يتم إضافة طابع زمني
const timestamp = new Date().getTime();
const response = await fetch(`./data/${bookId}/chapter_${chapterNum}.json?v=${timestamp}`);
```

هذا يعني:
- ✓ لا حاجة لمسح ذاكرة التخزين المؤقت يدوياً
- ✓ التحديثات تظهر فوراً للمستخدمين
- ✓ كل طلب يجلب أحدث نسخة من البيانات

---

## 📁 هيكل المشروع

```
exam-platform-v2/
├── data/
│   ├── books.json                 # فهرس جميع الكتب
│   ├── law_penal/
│   │   ├── chapter_1.json
│   │   ├── chapter_2.json
│   │   └── ...
│   ├── law_admin/
│   │   ├── chapter_1.json
│   │   └── ...
│   └── ...
├── engine/
│   └── examEngine.js              # محرك الاختبارات
├── index.html                     # الصفحة الرئيسية
├── app.js                         # منطق التطبيق
├── style.css                      # التنسيق
├── update_questions.py            # أداة التحديث
└── DEVELOPMENT.md                 # هذا الملف
```

---

## 🎨 تحسين الأداء

### 1. تقليل حجم الملفات

استخدم أسماء قصيرة للخيارات:

```json
// ✓ جيد
"options": ["نعم", "لا", "ربما", "غير واضح"]

// ✗ سيء (طويل جداً)
"options": [
  "هذا هو الخيار الأول الذي يمثل الإجابة الصحيحة",
  "هذا هو الخيار الثاني",
  ...
]
```

### 2. تنظيم الأسئلة

رتب الأسئلة حسب الصعوبة:
- الفصل الأول: أسئلة سهلة (تعريفات)
- الفصول الوسطى: أسئلة متوسطة
- الفصول الأخيرة: أسئلة صعبة (حالات عملية)

### 3. شرح واضح

اجعل الشروحات مختصرة وواضحة:

```json
// ✓ جيد
"explanation": "المادة 5 تنص على..."

// ✗ سيء
"explanation": "هذا يتعلق بموضوع معقد جداً في القانون وله تاريخ طويل..."
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الأسئلة لا تظهر

**الحل:**
1. تأكد من أن اسم المجلد يبدأ بـ `law_`
2. تأكد من أن ملف الفصل يسمى `chapter_1.json` (وليس `ch1.json`)
3. قم بتشغيل `python3 update_questions.py validate`

### المشكلة: الكاش لا يتحدث

**الحل:**
- المنصة تستخدم Cache Busting تلقائياً
- لا تحتاج لفعل شيء، التحديثات تظهر فوراً

### المشكلة: خطأ في JSON

**الحل:**
```bash
# تحقق من صحة JSON
python3 -m json.tool data/law_penal/chapter_1.json
```

---

## 📊 مثال كامل

### إضافة كتاب "القانون التجاري"

```bash
# 1. إنشاء المجلد
mkdir -p data/law_commercial

# 2. إنشاء الفصل الأول
cat > data/law_commercial/chapter_1.json << 'EOF'
{
  "questions": [
    {
      "id": 1,
      "q": "ما هو القانون التجاري؟",
      "options": [
        "مجموعة القواعد التي تنظم العلاقات التجارية",
        "القانون المدني فقط",
        "القانون الجنائي",
        "القانون الإداري"
      ],
      "correct": 0,
      "explanation": "القانون التجاري يختص بتنظيم العلاقات التجارية والعقود التجارية"
    }
  ]
}
EOF

# 3. تحديث الفهرس
python3 update_questions.py update

# 4. التحقق
python3 update_questions.py validate
```

---

## 🚀 نصائح للإنتاجية

1. **استخدم محرر نصوص جيد:** VS Code مع إضافة JSON Formatter
2. **اختبر محلياً:** افتح `index.html` في المتصفح قبل الرفع
3. **احفظ بانتظام:** استخدم `git commit` بعد كل تحديث
4. **وثّق الأسئلة:** أضف تعليقات في الشرح للأسئلة المعقدة

---

## 📝 الترخيص

جميع الأسئلة والمحتوى © 2026 منصة الاختبارات القانونية

---

**آخر تحديث:** 3 يوليو 2026
