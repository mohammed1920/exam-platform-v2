# سياسة الأمان والخصوصية

## ما تم إصلاحه

- لا يوجد GitHub Token داخل ملفات المشروع أو داخل JavaScript المعلن للمستخدم.
- لوحة الإدارة لا تحفظ Token في `localStorage` أو `sessionStorage`؛ يبقى في الذاكرة أثناء جلسة الصفحة فقط.
- لوحة الإدارة تتحقق من حساب GitHub المرتبط بالـ Token عبر `/user` بدل كشف اسم المالك في الكود.
- الموقع العام لا ينشر `admin.html` ولا ملفات Python ولا تقارير التدقيق ولا ملفات CI؛ Workflow النشر يبني `dist/` عامًا فقط.
- تم منع إدخال محتوى الكتب/الأسئلة/تقارير الذكاء الاصطناعي مباشرة في HTML دون escaping في المسارات الحساسة.
- الروابط الخارجية في الموقع العام لا تُقبل إلا عبر HTTPS.
- لا يوجد في المشروع كود يقرأ IP الزائر أو يخزنه في `data/` أو `localStorage`.
- عداد الزيارات الخارجي لا يعرض IP للزائر، لكن مزود العداد وخدمة الاستضافة قد يملكان سجلات شبكة خاصة بهما؛ هذه ليست بيانات محفوظة في المستودع.

## صلاحيات GitHub الموصى بها

استخدم **Fine-grained Personal Access Token** مخصصًا لهذا المستودع فقط، وبأقل صلاحية لازمة: `Contents: Read and write`.
لا تستخدم Classic PAT بصلاحيات واسعة إذا لم تكن مضطرًا.

## حد أمني مهم

`admin.html` هو واجهة متصفح ثابتة، لذلك لا يمكن جعلها نظام مصادقة حقيقيًا بمجرد وضع كلمة مرور داخل JavaScript؛ أي سر موجود في JavaScript يمكن استخراجه. لهذا الإصدار لا نضع كلمة مرور أو Token داخل الكود أصلًا.

إذا أردت حماية إدارية أقوى من GitHub Token اليدوي، فالخطوة الصحيحة لاحقًا هي نقل عمليات الكتابة إلى Backend خاص (Vercel/خادم) واستخدام جلسة HttpOnly أو GitHub App، مع إبقاء مفاتيح GitHub وAI في Environment Variables على الخادم فقط.

## قبل نشر المستودع

لخصوصية المصدر نفسه، اجعل مستودع GitHub **Private** إذا كانت خطتك تسمح بذلك. نشر GitHub Pages يمكن أن يبقى عامًا بينما يبقى كود المستودع غير متاح للعامة، وفق إعدادات وخطة GitHub المناسبة.
