# تشغيل لوحة الإدارة بأمان

1. شغّل المشروع محليًا عبر خادم HTTP:
   ```bash
   python -m http.server 8000
   ```
2. افتح `admin.html` محليًا.
3. عند الطلب، أدخل Fine-grained GitHub Token محدودًا بالمستودع.
4. بعد إغلاق/تحديث الصفحة سيُطلب Token من جديد؛ لا يتم تخزينه.
5. لا تضع Token في `admin.html` أو أي ملف JavaScript أو JSON.

## لماذا؟

لوحة الإدارة تنفذ الكتابة على GitHub مباشرة من المتصفح في هذا الإصدار. لذلك لا يمكن اعتبار صفحة الإدارة العامة وسيلة مصادقة بحد ذاتها. الأمان الحقيقي هنا يعتمد على إبقاء Token خارج الملفات واستخدام Token محدود الصلاحيات.

## ما الذي يُنشر للمستخدم؟

Workflow النشر ينشئ `dist/` ويضم فقط ملفات الموقع العامة. لا يتم نشر:
- `admin.html`
- ملفات Python
- تقارير QA والإملاء
- ملفات GitHub Actions
- ملفات التوثيق الداخلية

لكن تذكّر: إذا كان مستودع GitHub نفسه Public، فالمستخدم يستطيع رؤية ملفات المستودع من GitHub حتى لو لم تُنشر في Pages. لإخفاء المصدر فعلًا، اجعل المستودع Private.
